import { TipFilterPipe } from './tip-filter.pipe';

describe('TipFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TipFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
