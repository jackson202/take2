import { RivalTeamFinderPipe } from './rival-team-finder.pipe';

describe('RivalTeamFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new RivalTeamFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
