import { FilterGamePipe } from './filter-game.pipe';

describe('FilterGamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGamePipe();
    expect(pipe).toBeTruthy();
  });
});
